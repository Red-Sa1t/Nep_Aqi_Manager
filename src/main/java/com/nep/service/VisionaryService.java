package com.nep.service;

public interface VisionaryService {
    /**
     * @param loginCode
     * @param password
     * @return
     */
    public boolean login(String loginCode, String password);
}
